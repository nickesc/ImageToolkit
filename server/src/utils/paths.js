class Paths{
    static env = {
        env: "./.env",
        prod: "./.env.prod",
        dev: "./.env.dev",
        test: "./.env.test"
    }

    static app = {
        app: "./src/app"
    }
}

module.exports = Paths;
