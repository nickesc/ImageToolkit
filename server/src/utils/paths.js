class Paths{
    static env = {
        env: "./server/.env",
        prod: "./server/.env.prod",
        dev: "./server/.env.dev",
        test: "./server/.env.test"
    }

    static app = {
        app: "./src/app"
    }
}

module.exports = Paths;
